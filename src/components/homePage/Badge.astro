---
export interface Props {
  communityImg: string;
}

const { communityImg } = Astro.props;
---

<div
  class="w-12 h-12 transform duration-500 hover:bg-blue text-2xl hover:-translate-y-3 content-center"
>
  <img class="h-full" src={communityImg} />
</div>
